// Represents a checkout or return event
public class Transaction {
    private Book book;
    private Student student;
    private Date dueDate;
    private Date checkoutDate;

    public Transaction(Book book, Student student, Date checkoutDate, Date dueDate) {
        // Initialize record
    }
}
