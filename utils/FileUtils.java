import java.util.*;
import java.io.*;

public class FileUtils {

    // Loads all books from "books.txt" and returns them as a list of Book objects
    public static List<Book> loadBooks() {

    }

    // Saves all current books back to "books.txt" (overwrites the file)
    public static void saveBooks(List<Book> books) {

    }

    // Appends a single new book entry to the end of "books.txt"
    public static void appendBook(Book book) {
        // Write book.toString() + newline
    }

    // Appends a record of borrowing/returning to "transactions.txt"
    public static void appendTransaction(String transactionDetails) {
        // Write transactionDetails (could include book title, borrower, date, etc.)
    }
}
